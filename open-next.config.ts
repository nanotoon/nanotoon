import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
```

Save it as `open-next.config.ts` in `C:\Users\Clyde\Desktop\nanotoon\`

**Important:** When saving in Notepad, change "Save as type" to "All Files" so it doesn't add `.txt` to the end.

Then push again:
```
git add .
```
```
git commit -m "add open-next config"
```
```
git push